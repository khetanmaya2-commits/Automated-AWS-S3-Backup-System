This is a text file and the backup is stored in aws s3 using ci/cd pipeline 
and now i have connected the s3 with a lambda function where as soon as
the backup is uploaded to s3 it triggers the lambda function.